const intitialState = {
    posts: [],
    post: {},
    loading: false
}

export default function(state = intitialState, action ) {
    switch(action){
        default:
            return state
    }
}